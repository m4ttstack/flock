## New

- ⌘1, ⌘2 and ⌘3 launch the new-pane buttons (rt cd, Claude, Codex) in the focused pane. Each button shows its shortcut when the pane is wide enough.
- The command palette offers Claude, Codex and rt cd for the focused pane. They run only when the pane's shell is at its prompt.
