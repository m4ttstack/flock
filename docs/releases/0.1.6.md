## Fixes

- A right-click in a focused pane goes to its program again when that program uses the mouse. Option-right-click still opens flock's menu, and so does a right-click in a plain shell or an unfocused pane.
- The rt modal keeps its loader up until the rt program has drawn, so the shell no longer flashes through on the way in.
