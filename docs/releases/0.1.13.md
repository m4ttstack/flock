## New

- rt cd and rt nav work in a pane running Claude Code. Picking a folder stashes any draft and types `/cd <path>` into Claude instead of opening a new pane. The command palette offers rt cd there too.
